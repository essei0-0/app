# setup:
#   addons:
#     - plan: heroku-postgresql
#       as: DATABASE
#   config:
#     S3_BUCKET: my-example-bucket
build:
  docker:
    web: front/Dockerfile
    worker: back/Dockerfile
  # config:
  #   RAILS_ENV: development
  #   FOO: bar
# release:
#   command:
#     - ./deployment-tasks.sh
#   image: worker
run:
  web: npm run dev
  worker: rails s -b 0.0.0.0